<div
  data-controller="nested-many"
  data-nested-many-association-name-value="<%= attribute_name %>"
>
  <%= render(Uchi::Flowbite::Input::Label.new(attribute: field.name, form: form)) { label } %>

  <div class="space-y-3 mt-2" data-nested-many-target="container">
    <%# Render existing associated records %>
    <% associated_records.each_with_index do |assoc_record, index| %>
      <div
        class="border border-default-medium rounded-base p-4 bg-neutral-primary-medium relative"
        data-nested-many-target="item"
        data-new-record="<%= assoc_record.new_record? %>"
      >
        <%= form.fields_for(field.name, assoc_record, child_index: index) do |nested_form| %>
          <%# Hidden field for ID (existing records only) %>
          <% unless assoc_record.new_record? %>
            <%= nested_form.hidden_field :id %>
          <% end %>

          <%# Hidden field for _destroy %>
          <%= nested_form.hidden_field :_destroy, value: "0", data: {nested_many_target: "destroyField"} %>

          <div class="grid grid-cols-1 gap-4 mb-3">
            <%# Render each configured nested field %>
            <% nested_field_components.each do |nested_field| %>
              <div>
                <%= render(nested_field.edit_component(
                  form: nested_form,
                  repository: associated_repository,
                  label: associated_repository.translate.field_label(nested_field),
                  hint: nil
                )) %>
              </div>
            <% end %>
          </div>

          <%# Remove button %>
          <div class="absolute top-2 right-2">
            <button
              type="button"
              class="text-danger hover:text-danger-emphasis p-1 rounded hover:bg-danger/10"
              data-action="nested-many#removeItem"
              title="<%= I18n.t("uchi.field.nested_many.remove_item", default: "Remove") %>"
            >
              <svg
                class="w-5 h-5"
                aria-hidden="true"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
              >
                <path
                  stroke="currentColor"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 7h14m-9 3v8m4-8v8M10 3h4a1 1 0 0 1 1 1v3H9V4a1 1 0 0 1 1-1ZM6 7h12v13a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1V7Z"
                />
              </svg>
            </button>
          </div>
        <% end %>
      </div>
    <% end %>

    <%# Template for new records (hidden, cloned by Stimulus) %>
    <template data-nested-many-target="template">
      <div
        class="border border-default-medium rounded-base p-4 bg-neutral-primary-medium relative"
        data-nested-many-target="item"
        data-new-record="true"
      >
        <%= form.fields_for(field.name, reflection.klass.new, child_index: "NEW_RECORD") do |nested_form| %>
          <%# Hidden field for _destroy (new records don't have ID) %>
          <%= nested_form.hidden_field :_destroy, value: "0", data: {nested_many_target: "destroyField"} %>

          <div class="grid grid-cols-1 gap-4 mb-3">
            <%# Render each configured nested field %>
            <% nested_field_components.each do |nested_field| %>
              <div>
                <%= render(nested_field.edit_component(
                  form: nested_form,
                  repository: associated_repository,
                  label: associated_repository.translate.field_label(nested_field),
                  hint: nil
                )) %>
              </div>
            <% end %>
          </div>

          <%# Remove button %>
          <div class="absolute top-2 right-2">
            <button
              type="button"
              class="text-danger hover:text-danger-emphasis p-1 rounded hover:bg-danger/10"
              data-action="nested-many#removeItem"
              title="<%= I18n.t("uchi.field.nested_many.remove_item", default: "Remove") %>"
            >
              <svg
                class="w-5 h-5"
                aria-hidden="true"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
              >
                <path
                  stroke="currentColor"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 7h14m-9 3v8m4-8v8M10 3h4a1 1 0 0 1 1 1v3H9V4a1 1 0 0 1 1-1ZM6 7h12v13a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1V7Z"
                />
              </svg>
            </button>
          </div>
        <% end %>
      </div>
    </template>
  </div>

  <div class="mt-3">
    <%= render(Uchi::Flowbite::Button.new(
      type: "button",
      style: :secondary,
      size: :sm,
      data: {action: "nested-many#addItem"}
    )) do %>
      <svg
        class="w-4 h-4 inline-block mr-1 -ml-1"
        aria-hidden="true"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
      >
        <path
          stroke="currentColor"
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M5 12h14m-7 7V5"
        />
      </svg>
      <%= I18n.t("uchi.field.nested_many.add_item", default: "Add #{field.name.to_s.singularize.humanize}") %>
    <% end %>
  </div>

  <% if hint.present? %>
    <%= render(Uchi::Flowbite::Input::Hint.new(attribute: field.name, form: form)) { hint } %>
  <% end %>
</div>
